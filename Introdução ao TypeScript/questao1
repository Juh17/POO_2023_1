1 - Qual a diferença entre tipagem dinâmica e tipagem estática?
  
  R = A tipagem estática não permite ao desenvolvedor alterar otipo da variável depois de declarada. Normalmente a verificação de tipo é feita em tempo de compilação. Já a tipagem dinâmica escolhe o tipo de dado de acordo com o valor atribuído à variável em tempo de execução dinamicamente.
